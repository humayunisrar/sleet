import React from 'react'
import styles, { layout } from "../style";

const Business = () => (
 
  <section id="about" className={layout.section}>
  <div id="about" className={`text-center items-center ${layout.section} ${layout.sectionInfo}`}>
    <h2 className={`text-magenta text-center font-normal ${styles.heading2}`} >
     <span className='text-magenta text-center font-normal'>Our innovations are what aid us to provide you with a better and superior service</span>
     </h2>
    
  </div>
  
  </section>
  

)
export default Business