import React from "react";

const Read = ({ styles }) => (
  <button type="button" className={`py-4 px-6 font-poppins font-medium text-[18px] text-white bg-primary rounded-[10px] outline-none ${styles}`}>
    Get in Touch
  </button>
);

export default Read;